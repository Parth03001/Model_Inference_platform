## Requirement for plaform for model inferencing on video stream

- This URL:rtsp://admin:mahindra%40123@10.5.12.161/streaming/channels/101

### requirement
- first go through the reference code where have the same inferencing system for pp kit montinoring where we just upload the model weights then used that model to inference on the video stream for given url where you can see that how the use the url how  and infernce model on that  same way i want to use that 

- after the referencing the code see what i want is the beautiful UI with framer motion and animation which will have landing page like that where we will that just upload weights and add camera url so it will single camera for mutliple camera  add all connection then fetch video stream infermce on that then store that result 

#### Application requirement

- for the storage i want the postgresql
- for the backedn i want the fast api and python
- for the frontedn use react js for creating the app use create-react-app 
- rule for the frontend development is that keep the code component wise make the each component css and js seperate and don not use tailwind css which is incline 